{
  "name": "desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri v2 desktop/tablet wrapper for the POS frontend",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}
